# Utilities

Shared frontend helpers (formatters, validators, constants) live here.

- `formatDuration` — formats a whole number of seconds as `m:ss` (or
  `h:mm:ss`), used by `ParticipantsCard` for the Speaking column.
